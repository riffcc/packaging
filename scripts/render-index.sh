#!/usr/bin/env bash
set -euo pipefail

echo "Static front page is checked in directly for now."
echo "Later this script should render package tables from config/*.json into public/index.html and per-package pages."
